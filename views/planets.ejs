<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Planetas</title>

            <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">

    <!-- Font Awesome CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
</head>
<body class="bg-dark text-light">

    <div class="container mt-5">
        <h1 class="display-4 mb-4"><i class="fas fa-globe"></i> Informações dos Planetas</h1>

        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Diâmetro</th>
                    <th>Clima</th>
                    <th>Gravidade</th>
                    <th>Período Orbital</th>
                    <th>População</th>
                    <th>Período de Rotação</th>
                    <th>Água Superficial</th>
                    <th>Terreno</th>
                </tr>
            </thead>
            <tbody>
                <% planets.forEach(planet => { %>
                    <tr>
                        <td><%= planet.name %></td>
                        <td><%= planet.diameter %></td>
                        <td><%= planet.climate %></td>

                        <td>
                            <% if (planet.gravity.toLowerCase() === 'n/a') { %>
                                no information
                            <% } else { %>
                                <%= planet.gravity %>
                            <% } %>
                        </td>

                        <td><%= planet.orbital_period %></td>


                        <td>
                            <% if (planet.population.toLowerCase() === 'unknown') { %>
                                no information
                            <% } else { %>
                                <%= planet.population %>
                            <% } %>
                        </td>
                        
                        <td><%= planet.rotation_period %></td>

                        <td>
                            <% if (planet.surface_water.toLowerCase() === 'unknown') { %>
                                no information
                            <% } else { %>
                                <%= planet.surface_water %>
                            <% } %>
                        
                        </td>

                        <td><%= planet.terrain %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <!-- Botão para retornar à rota principal -->
        <a href="/" class="btn btn-warning"><i class="fas fa-arrow-left"></i> Voltar para a Rota Principal</a>
    </div>
</body>
</html>