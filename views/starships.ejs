<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Starships Information</title>

        <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta3/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-eOJMYsd53ii+scO/bJGFsiCZc+5NDVN2yr8+0RDqr0Ql0h+rP48ckxlpbzKgwra6" crossorigin="anonymous">

    <!-- Font Awesome CSS -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.1/css/all.min.css">
</head>
<body class="bg-dark text-light">

    <div class="container mt-5">
        <h1 class="display-4 mb-4"><i class="fas fa-space-shuttle text-warning"></i> Informações das Naves Estelares</h1>

        <table class="table table-dark table-striped">
            <thead>
                <tr>
                    <th>Nome</th>
                    <th>Modelo</th>
                    <th>Classe</th>
                    <th>Comprimento</th>
                    <th>Capacidade de Carga</th>
                    <th>Velocidade Máxima (Atmosférica)</th>
                    <th>Passageiros</th>
                    <th>Equipe</th>
                    <th>Rating do Hyperdrive</th>
                    <th>Consumíveis</th>
                </tr>
            </thead>
            <tbody>
                <% starships.forEach(starship => { %>
                    <tr>
                        <td><%= starship.name %></td>
                        <td><%= starship.model %></td>
                        <td><%= starship.starship_class %></td>
                        <td><%= starship.length %></td>
                        <td><%= starship.cargo_capacity %></td>
                        <td>
                            <% if (starship.max_atmosphering_speed.toLowerCase() === 'n/a') { %>
                                no information
                            <% } else { %>
                                <%= starship.max_atmosphering_speed %>
                            <% } %>
                        
                        </td>
                        <td><%= starship.passengers %></td>
                        <td><%= starship.crew %></td>
                        <td><%= starship.hyperdrive_rating %></td>
                        <td><%= starship.consumables %></td>
                    </tr>
                <% }); %>
            </tbody>
        </table>

        <a href="/" class="btn btn-warning"><i class="fas fa-arrow-left"></i> Voltar para a Página Principal</a>
    </div>
</body>
</html>
